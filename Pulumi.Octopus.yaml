config:
  aws:region: "#{AWS.region}"
  pulumi-eks-hello-world:vpcName: "#{vpcName}" 
  pulumi-eks-hello-world:clusterName: "#{clusterName}" 
  pulumi-eks-hello-world:runningViaOctopus: true
  pulumi-eks-hello-world:isMinikube: false