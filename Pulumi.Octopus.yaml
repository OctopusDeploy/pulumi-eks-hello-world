config:
  aws:region: "#{AWS.region}"
  pulumi-eks-hello-world:vpcName: "#{vpcName}" 
  pulumi-eks-hello-world:clusterNamePrefix: "#{clusterNamePrefix}" 
  pulumi-eks-hello-world:redisImage: "#{redis.image}"
  pulumi-eks-hello-world:guestbookImage: "#{guestbook.image}"